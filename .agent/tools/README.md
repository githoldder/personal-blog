# Tool Registry Guide

Tool contracts live here for human-readable guidance.

Machine-readable registry:

- `sense/registry/tools.json`

Rules:

- Prefer tools with structured output.
- Prefer deterministic scripts over repeated agent reasoning.
- Register any tool that becomes part of a repeatable workflow.
- Mark external side effects and permission levels explicitly.
