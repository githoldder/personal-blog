# Large Asset CDN Strategy

Large PDFs, resume packages, deck exports and image archives may later move to Cloudflare R2 or another CDN.

## Rules

- Local evidence remains private unless explicitly approved.
- Generated public assets can be uploaded only after release approval.
- CDN deletion and rollback commands must be recorded in a deployment record.
- Public pages should use stable asset URLs and preserve local fallback builds.
