# Admin Manual

The admin workflow is local-first:

1. Review source records and evidence locally.
2. Update `content/publish-manifest.json` for public eligibility.
3. Run preview and build commands.
4. Run V-model tests.
5. Commit and push only after local verification.

Admin routes are not a production CMS. They are an operator console for a trusted local maintainer.
