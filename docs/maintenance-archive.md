# Maintenance Archive

Recurring commands:

```bash
npm run build:assets
npm run validate:prd
npm run validate:pipeline
npm test
npm run verify
```

Known rules:

- Do not expose private evidence by default.
- Do not deploy from CI.
- Keep PRDs, context memory, and implementation artifacts synchronized.
