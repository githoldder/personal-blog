# LLM Agent Workflows

Allowed local workflow designs:

- Summarize selected public candidate notes.
- Suggest tags and wiki-links.
- Detect privacy risk before publication.
- Draft release notes and review comments.

Agent output is advisory. It must not silently rewrite source files, change publish status, deploy, or upload private content.
